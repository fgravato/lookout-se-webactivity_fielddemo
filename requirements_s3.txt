# Additional requirements for Lookout S3 Logger
# Install with: pip install -r requirements_s3.txt

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Existing dependencies (if not already installed)
requests>=2.31.0
python-dotenv>=1.0.0
tabulate>=0.9.0

# Optional: For enhanced logging and monitoring
structlog>=23.1.0
prometheus-client>=0.17.0

# Development/Testing dependencies (optional)
pytest>=7.4.0
moto>=4.2.0  # For AWS mocking in tests
